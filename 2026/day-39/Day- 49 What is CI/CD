Day 39 – What is CI/CD?

Objective

Today I focused on understanding the core concepts of CI/CD before creating any pipelines.

What I Learned

Why CI/CD?

- Reduces manual deployment errors
- Detects bugs early
- Automates build, test, and deployment
- Makes software delivery faster and more reliable

Team Problems Without CI/CD

- Merge conflicts
- Human errors during deployment
- "It works on my machine" issues
- Slow and risky releases

CI vs CD

Continuous Integration (CI)

Developers frequently push code to a shared repository. Every push automatically triggers build and testing to catch issues early.

Continuous Delivery

After CI, the application is automatically prepared for deployment. Production release still requires manual approval.

Continuous Deployment

Every successful build is automatically deployed to production without manual intervention.

Pipeline Anatomy

- Trigger – Starts the pipeline
- Stage – Logical phase (Build, Test, Deploy)
- Job – A unit of work inside a stage
- Step – A single command or action
- Runner – Machine that executes the workflow
- Artifact – Output generated by the pipeline

Simple CI/CD Pipeline

Developer
   │
   ▼
Git Push
   │
   ▼
GitHub Actions
   │
   ├── Build
   ├── Test
   ├── Docker Build
   └── Deploy to Staging

Open Source Workflow Study

Repository Explored: Kubernetes

Observations:

- Triggered on Push and Pull Request
- Contains multiple jobs
- Performs code checkout, build, testing, and validation

Key Takeaways

- CI/CD is a practice, not just a tool.
- A failed pipeline helps detect problems before deployment.
- Automation improves software quality and delivery speed.

#90DaysOfDevOps #Day39 #CICD #GitHubActions #DevOps #TrainWithShubham